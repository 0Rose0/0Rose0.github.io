$(document).ready(function(){
  //JQuery(document) instead?//

             
 // $("button").remove();
 // $(".uploadbutton").remove();
  
  
  
 

 
  
  $("button").on("click", function () {
      $("button").remove();
  
    
  });
});
